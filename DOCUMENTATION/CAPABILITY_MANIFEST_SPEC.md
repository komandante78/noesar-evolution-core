
# Capability manifest specification

The canonical manifest schema is `CAPABILITIES_AND_SDK/schemas/capability-manifest.schema.json`. A manifest declares identity, version, publisher, entry points, permissions, compatibility, resources, network/data access and lifecycle metadata. Manifests are data declarations, not authority grants: policy and user authorization remain mandatory.
