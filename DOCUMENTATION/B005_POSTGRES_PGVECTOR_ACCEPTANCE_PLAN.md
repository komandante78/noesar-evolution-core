# B005 — PostgreSQL/pgvector live acceptance

The twelve migrations, production-attestation ledger, RLS policies, hash-chain
guards, repository adapter and operational tooling are included. Execute them
against an isolated PostgreSQL 18 + pgvector instance during installation, then
record migration, RLS, concurrency, backup/restore and rollback evidence.
