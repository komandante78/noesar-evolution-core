# SPDX-License-Identifier: AGPL-3.0-or-later
#
# D-0255: SESS-001, the Session Proof assembler (session-proof.mjs) wired to a real
# plan()->approve() run via GET /api/v1/workspace-actions/:id/session-proof. See
# DECISION_LOG.md for the ten fields and the three small wiring gaps this exposed and fixed
# (hypotheses/request stored on the run, capability.denied recorded, egress sampled).

FROM noesar-evolution:phase4-arch008-execute-rust-mirror

LABEL org.noesar.phase="4-sess001-session-proof"
LABEL org.noesar.sess-001="built"

USER root
COPY --chown=10001:10001 services/reference-control-plane/ /opt/noesar/services/reference-control-plane/
USER 10001:10001
