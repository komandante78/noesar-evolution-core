# SPDX-License-Identifier: AGPL-3.0-or-later
#
# D-0259: SESS-002 (the replay engine: recompute a run's decision layer, or resubmit its
# captured fixture pack to the external provider's own /v1/replay) + SESS-003 (replay a
# historical, externally-supplied fixture against the CURRENT code's policy). See
# DECISION_LOG.md for the two routes and the DECISION_SURFACES fix found building this.

FROM noesar-evolution:phase4-sess001-session-proof

LABEL org.noesar.phase="4-sess002-003-replay"
LABEL org.noesar.sess-002="built"
LABEL org.noesar.sess-003="built"

USER root
COPY --chown=10001:10001 services/reference-control-plane/ /opt/noesar/services/reference-control-plane/
USER 10001:10001
