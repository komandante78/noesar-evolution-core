# Canonical React WebUI Target

This directory reserves the canonical TypeScript/React application boundary.
The executable static WebUI is used for source-gate acceptance because the
package registry was unavailable during generation. Before final ZIP 1, versions
must be pinned, lockfiles generated, licenses audited and the React application
must reproduce the approved static behaviour without placeholders.
