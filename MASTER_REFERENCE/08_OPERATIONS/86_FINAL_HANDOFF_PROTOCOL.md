# Final Handoff Protocol

Freeze release commit and locks; build in isolation; generate provenance/BOMs;
run exact-artifact acceptance; remediate and rebuild; independently verify;
generate ZIPs 1–4; generate ZIP 5 last with hashes of 1–4; sign and verify;
record Owner acceptance. Any later hash change invalidates final status.
