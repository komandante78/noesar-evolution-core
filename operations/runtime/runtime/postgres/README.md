# PostgreSQL Operations

The scripts require an operator-supplied `NOESAR_DATABASE_URL` and explicit
confirmation. They install no packages and are not executed during packaging.

```text
MIGRATION_STATIC_VERIFY=PASS
PSQL_EXECUTION=NOT_EXECUTED
BACKUP_RESTORE_DRILL=NOT_EXECUTED
POSTGRES_ATTESTATION=NOT_ISSUED
```
