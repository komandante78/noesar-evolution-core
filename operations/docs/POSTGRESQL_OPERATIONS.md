# PostgreSQL Operations V0.5.0

The intended sequence is static verification, backup, deterministic plan,
explicit migration authorization, ten migration applications, migration
ledger recording, security-acceptance query, role/RLS adversarial testing,
repository activation, backup/restore drill and signed attestation.

PostgreSQL and pgvector execution remain `NOT_EXECUTED`.
