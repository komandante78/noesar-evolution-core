# Production Promotion Gate

Promotion requires:

- compiled and tested canonical Rust authority;
- authenticated OS peer credentials and signed provenance;
- PostgreSQL 18 and pgvector execution;
- ten migrations, role separation, RLS and immutable-ledger acceptance;
- active repository and backup/restore evidence;
- production sandbox with signed escape-test evidence;
- executed Docker, Podman, Windows, macOS and Unraid matrix;
- production WebUI security and dependency audit;
- TUF, Sigstore and SLSA release trust;
- independent penetration test;
- zero blockers.
