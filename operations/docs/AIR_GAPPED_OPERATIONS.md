# Air-Gapped Operations

Use offline images, trust roots, capability/model packages, licenses,
update metadata and release manifests. Verify every artifact before
activation. Internet access is not a runtime dependency.
