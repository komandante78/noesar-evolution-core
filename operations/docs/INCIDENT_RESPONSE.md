# Incident Response

```text
detect -> authenticate responder -> contain -> preserve evidence
-> revoke session/publisher/package/ticket -> recover -> verify -> report
```

Incident classes include Owner compromise, IPC replay, peer-credential
bypass, binary or schema substitution, migration/RLS failure, malicious
capability, execution-ticket theft, sandbox escape and update compromise.
