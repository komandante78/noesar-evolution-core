# Measurements

Regenerated by `curve.mjs` at the end of every `run-bench.sh`. Latest run: `bm25-live`, 2026-09-10 02:54 UTC.

![three panels](PROGRESS.svg)

## Where we are

Best localisation: **26/155 (16.8%)**, in run `bm25-live`.
In the latest run **43 of 155 instances** had `interpret` degraded: that much variance is not under control.

| run | date | top-5 | % | degraded | conditions |
|---|---|---|---|---|---|
| `baseline` | 2026-09-09 | 13/155 | 8.4 | — | **unrecorded** |
| `after-idf` | 2026-09-09 | 10/155 | 6.5 | — | **unrecorded** |
| `after-onewalk` | 2026-09-10 | 10/155 | 6.5 | — | **unrecorded** |
| `rep1` | 2026-09-10 | 10/155 | 6.5 | — | **unrecorded** |
| `rep2` | 2026-09-10 | 10/155 | 6.5 | — | **unrecorded** |
| `with-model-onewalk` | 2026-09-10 | 14/155 | 9.0 | — | **unrecorded** |
| `capture-with-model` | 2026-09-10 | 13/155 | 8.4 | 44 | recorded |
| `bm25-live` | 2026-09-10 | 26/155 | 16.8 | 43 | recorded |

## What can be compared

- `capture-with-model` -> `bm25-live`: **+13** out of 155, same reasoning conditions.

6 of 8 runs predate the fingerprint (`baseline`, `after-idf`, `after-onewalk`, `rep1`, `rep2`, `with-model-onewalk`): they are comparable with nothing, and a difference between them would measure the conditions rather than the change.

## What the two empty panels are waiting for

1. **Size**: a *resolve rate* of our own. Today we measure whether the right file is among the top five; competitors publish how many instances they resolve. Those are different quantities and do not share an axis. It needs `bench.mjs` to write `resolved`, and `rivals.json` to carry rows with a source and a date.
2. **Steps**: the edit -> test -> repair loop. Every plan measured is **a single step**, so the x axis holds one value and no curve exists.

Both panels fill themselves in when that data appears. Nothing here is redrawn by hand.
