# Rollback

The exact ZIP 2 V0.3.0 archive is embedded under `BASELINE_V0_3_0`.

Do not restore the legacy V0.3.0 PostgreSQL migration set into a new database.
It is retained only for forensic comparison and package rollback analysis.
