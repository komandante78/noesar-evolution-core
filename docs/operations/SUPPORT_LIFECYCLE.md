# Support Lifecycle

V0.5.0 is not a production support baseline. Diagnostic evidence must
exclude setup tokens, passwords, TOTP secrets, sessions, master keys,
prompts, documents, memory, model inputs/outputs and private keys.
